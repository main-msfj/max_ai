# AI Harness Research · Flask server

Desde esta carpeta:

```bash
uv run flask --app app run --debug
```

Abre <http://127.0.0.1:5000> en el navegador.

Opcionalmente:

```bash
uv run flask --app app run --host 0.0.0.0 --port 8000 --debug
```
