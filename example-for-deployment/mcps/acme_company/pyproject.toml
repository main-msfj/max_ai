[project]
name = "acme-company-mcp"
version = "0.1.0"
description = "Acme AI HR MCP server (example)"
requires-python = ">=3.11"
dependencies = ["mcp==2.2.0", "pyjwt>=2.10", "python-dotenv>=1.2.2"]

[tool.uv]
package = false
