"""Data models of each core module (one file per module).

Import from the module itself (``core.model.compaction``) or from its
package (``core.compaction``). Nothing is re-exported here on purpose:
a hub importing every model is how import cycles start.
"""
