[project]
name = "maxai"
version = "0.1.0"
description = "A framework project for llm framework"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.135.1",
    "jinja2>=3.1.6",
    "jsonschema>=4.26.0",
    "mcp>=1.26.0",
    "mcp-server-fetch>=2025.4.7",
    "ollama>=0.6.1",
    "openai>=2.24.0",
    "opentelemetry-distro>=0.60b1",
    "opentelemetry-exporter-otlp>=1.39.1",
    "pydantic>=2.12.5",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "python-frontmatter>=1.1.0",
    "pyyaml>=6.0.3",
    "qdrant-client[fastembed]>=1.17.0",
    "redis>=7.2.0",
    "requests>=2.32.5",
    "tiktoken>=0.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["max_ai"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "hatchling>=1.29.0",
    "mcp>=1.26.0",
    "pyjwt>=2.11.0",
]

ui = [
    "uvicorn[standard]>=0.34.0",
]

